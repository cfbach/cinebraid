# CineBraid v6.6.4-studio.repair.1 — Handoff

This release consolidates the visual theme layer, repairs the overflowing feedback modal, and adds an Ollama `/api/generate` fallback when `/api/chat` returns an empty final response.
