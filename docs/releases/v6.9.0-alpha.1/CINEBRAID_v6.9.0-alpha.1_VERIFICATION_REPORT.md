# CineBraid 6.9.0-alpha.1 — verification basis

Final human dogfood acceptance and publication remain pending. No 6.9 tag, release, public main update or installer is claimed.

## Qualified parent, not an invented child result

- Integration commit: `b519b8aa613f39425f5b9c0d1f8ace0cd261c6b2`.
- Integration tree: `1ecf18515e3c8662ab2735ecb8b22bb1ad7d0ff1`.
- All accepted EV2-1–6 identities remain reachable in linear ancestry. EV2-6 remains `d88e79b558488a76e074b6f4df83743cc0f12f3a`, tree `550640a4513b6edf73814e46efe07faf4cd78329`.

Completed local automated qualification on that exact parent:

| Command | Recorded result |
|---|---|
| npm run check:ci | Passed; 118 registered command entries including aggregates |
| npm run check | Passed; 285 registered full-runner suites |
| npm run check:browser-gate | Passed; 46/46 required suites launched and passed, zero skips |
| npm run check:authority-browser | Passed separately |
| Local history credential/privacy scan | Passed; synthetic positive controls exercised |

These were local Windows executions, not hosted GitHub Actions results. They used disposable synthetic profiles/projects/media with blocked provider traffic. The final canonical census recorded 51 browser launches, 14 tracked data/project files byte-identical, all 28 reported disposable workspaces removed, and 9 settings locations unchanged by metadata-only checks. No real credential values were read or hashed for that census.

Results coverage included 390×844, 1280×720, 1440×900 and 1920×1080, pointer/keyboard operation, exact opener restoration, explicit approval, failed-save retry, uncertain completion, motion viewing, missing/foreign identity and narrow layouts. No screen-reader or cross-browser certification is claimed.

## Advisory evidence and deferred polish

The repository browser job remains advisory pending BROWSER_GATE_RUNNER_STABILITY_V1, although its canonical command is strict. Five existing quarantined suites launched and reproduced their recorded reasons; none was added or repinned to obtain a pass:

- H3 motion prompt editor fixture/selector.
- Legacy continuity repair-control wording.
- Hidden continuity workspace fixture.
- Frame-review fixture issuing no request.
- Frames-to-motion fixture without a matching passing sequence review.

A pre-existing continuity reveal helper's automatic focus in the parent-derived state view and rapid navigation away during its delayed callback remain unqualified. Results exact-opener restoration passed separately.

The oversized transient Approved treatment and mobile toast overlap remain accepted non-blocking polish.

## Mechanical version child

The child changes package/lock identity, derived title/cache stamps, changelog and these required packaging documents. Visible app identity derives from package.json through the existing release-identity owner. Optional build identity retains its truthful source-checkout fallback; no hardcoded self-referential commit or fabricated build date is introduced. Application version changes do not renumber project/schema markers.

Focused version consistency, syntax, packaging/document and committed-diff results for the exact child are recorded in the external owner preparation record after execution. This packaged document does not claim those results in advance, and complete CI/browser qualification is not rerun merely for the version stamp. Final human dogfood acceptance and publication remain pending.
